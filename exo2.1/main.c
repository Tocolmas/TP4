#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[])
{
    File *fichier = NULL;
    char texte[200];
    char filename[1024+1];

    fichier = fopen(filename.txt,w);

    printf ("quel texte voulez vous introduire dans le fichier\n");
    fgets (texte, 200, stdin);

    printf ("quel est le nom du fichier dans lequel il faut écrire\n");
    fgets (filename, 1024, stdin);

    echo "texte" > filemame.txt
    wc filename.txt


